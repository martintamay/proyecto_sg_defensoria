<p id="notice"><%= notice %></p>

<h1>Asistentes</h1>
<hr>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Defensor Encargado</th>
      <th>Correo</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @assistants.each do |assistant| %>
      <tr>
        <td><%= assistant.entity.full_name %></td>
        <td><%= assistant.lawyer.entity.full_name %></td>
        <td><%= assistant.user.email %></td>
        <td>
          <%= link_to (fa_icon 'search'), assistant, class: "btn btn-default" %>
          <%= link_to (fa_icon 'pencil'), edit_assistant_path(assistant), class: "btn btn-default" %>
          <%= link_to (fa_icon 'trash'), assistant, method: :delete, data: { confirm: 'Está seguro?' }, class: "btn btn-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to (fa_icon 'plus', text: 'Nuevo Asistente'), new_assistant_path, class: 'btn btn-primary' %>
