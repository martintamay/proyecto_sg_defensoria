<%= bootstrap_form_for(@entity) do |form| %>
  <% if entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entity.errors.count, "error") %> prohibited this entity from being saved:</h2>

      <ul>
      <% entity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <%= form.text_field :name, id: :entity_name, label: "Nombre" %>
      <%= form.text_field :last_name, id: :entity_last_name, label: "Apellido" %>
      <%= form.phone_field :phone, id: :entity_phone, label: "Teléfono" %>
      <%= form.date_select :birthdate, id: :entity_birthdate, label: "Nacimiento", start_year: 1850, use_month_numbers: true, end_year: 2050 %>
      <%= form.email_field :email, id: :entity_email, label: "Email" %>
      <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
        <%= fa_icon 'floppy-o', text: 'Guardar' %> Guardar
      <% end %>
      <%= link_to (fa_icon 'arrow-circle-left', text: 'Volver'), entities_path, class: "btn btn-default" %>

  </div>
<% end %>
