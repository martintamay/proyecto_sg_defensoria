<%= bootstrap_form_for(@entity) do |form| %>
  <% if entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entity.errors.count, "error") %> prohibited this entity from being saved:</h2>

      <ul>
      <% entity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="col-md-4"></div>
  <div class="col-md-5"><!-- Tamaño del formulario -->
 
 <!-- Formulario --> 
  <div class="panel panel-primary" >
        
        <div class="panel-body">
            <form class="form-inline">
    <div class="form-group">
      <%= form.text_field :name, id: :entity_name, label: "Nombre" ,class: "form-group"%>
      <%= form.text_field :last_name, id: :entity_last_name, label: "Apellido",class: "form-group"%>
      <%= form.phone_field :phone, id: :entity_phone, label: "Teléfono" ,class: "form-group"%>
      <%= form.date_select :birthdate, id: :entity_birthdate, label: "Nacimiento", start_year: 1850, use_month_numbers: true, end_year: 2050 ,class: "form-group"%>
      <%= form.email_field :email, id: :entity_email, label: "Email",class: "form-group"%>
      </div>
                  <%= link_to (fa_icon 'arrow-circle-left', text: 'Atras'),entities_path,class:  'btn btn-default pull-left'%>
                   
                     <%= button_tag(type: 'submit',class: "btn btn-primary pull-right") do %>
                        <%= fa_icon 'floppy-o' %> Guardar
                     <% end %> 
             
              <% end %>       
        
            </div>
        </div>
        <hr></hr>
      </div>
