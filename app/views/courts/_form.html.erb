<%= bootstrap_form_for(@court) do |form| %>
  <% if court.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(court.errors.count, "error") %> prohibited this court from being saved:</h2>

      <ul>
      <% court.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= form.text_field :name, id: :court_name, label: 'Nombre' %>
    <%= form.phone_field :phone, id: :court_phone, label: 'Teléfono' %>
    <%= form.text_field :address, id: :court_address, label: 'Dirección' %>
    <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
      <%= fa_icon 'floppy-o', text: 'Guardar' %> Guardar
    <% end %>
    <%= link_to (fa_icon 'arrow-circle-left', text: 'Volver'), courts_path, class: 'btn btn-default' %>
  </div>
<% end %>
