<p id="notice"><%= notice %></p>
<h1 align="center"> Reporte de Defensores </h1>
<%= link_to "PDF File", users_reports_path(format: 'pdf') %>

 <hr>    
     
<table id="table_di" class= "table table-hover  table-striped table-bordered" >
   <thead>
         <tr>
         	<th>Email</th>
            <th>Nombre</th>
            <th>Teléfono</th>
            <th>Nacimiento</th>
            <th>Email (Entidad)</th>
            <th>Tipo de Usuario</th>
            <th class="col-md-2"></th>
        </tr>
  </thead>

  <tbody>
    <% @usuario.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.entity.full_name %></td>
        <td><%= user.entity.phone %></td>
        <td><%= user.entity.birthdate %></td>
        <td><%= user.entity.email %></td>
        <td><%= user.type_user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
