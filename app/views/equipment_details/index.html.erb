
<p id="notice"><%= notice %></p>

<!-- PANEL PRINCIPAL -->
  <div class="box">
            <div class="box-header">
              <h3 class="box-title">LISTA DE EQUIPOS</h3>
               <div class="box-tools">
                  <ul class=" no-margin pull-right">
                    <%= link_to (fa_icon 'plus', text: 'Crear'), new_equipment_detail_path, class: 'btn btn-primary' %>
                  </ul> 
               </div>
            </div>
                         

              <!-- TABLA -->
            <div class="panel-body "  >   
                <table id="table_di" class= "table table-hover   table-bordered table-striped" >
                    <thead>  <!-- TITULOS DE LA TABLE -->
                        <tr>
                          <th>Caso</th>
                          <th>Defensor</th>
                          <th class="col-md-2"></th> 
                        </tr>
                      </thead>

                      <tbody>
                        <% @equipment_details.each do |equipment_detail| %>
                          <tr>
                            <td><%= equipment_detail.legal_case.name %></td>
                            <td><%= equipment_detail.user.entity.full_name %></td>
                            <td align="center" >
                              <div class="btn-group">
                                <%= link_to (fa_icon 'eye' ), equipment_detail ,class: "btn btn-default" %>
                                <%= link_to (fa_icon 'pencil'), edit_equipment_detail_path(equipment_detail),class: "btn btn-default" %>
                                <%= link_to (fa_icon 'trash'), equipment_detail, method: :delete, data: { confirm: 'Está seguro?' },class: "btn btn-default"  %>
                              </div>
                            </td>
                          </tr>
                        <% end %>
                    </tbody>
              </table>
</div>
</div>