
<!-- PANEL PRINCIPAL -->
<div class="box">
  <div class="box-header">
    <h3 class="box-title">EQUIPO</h3>
     <div class="box-tools">
        <ul class=" no-margin pull-right">
          <%= link_to (fa_icon 'plus', text: 'Crear'), new_equipment_detail_path, class: 'btn btn-primary' %>
        </ul>
     </div>
  </div>


 <hr>
<table id="table_di" class= "table table-hover  table-striped table-bordered" >
  <thead>
    <tr>
      <th>Caso</th>
      <th>Usuario</th>
    </tr>
  </thead>

  <tbody>
    <% @equipment_details.each do |equipment_detail| %>
      <tr>
        <td><%= equipment_detail.legal_case.name %></td>
        <td><%= equipment_detail.user.entity.full_name %></td>
        <td align="center" ><%= link_to (fa_icon 'eye'), equipment_detail, class: 'btn btn-default'  %>
         <%= link_to (fa_icon 'pencil'), edit_equipment_detail_path(equipment_detail), class: 'btn btn-default' %>
         <%= link_to (fa_icon 'trash'), equipment_detail, method: :delete, data: { confirm: 'Está seguro?' }, class: 'btn btn-default' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>