<%= bootstrap_form_for(@shift) do |form| %>
  <% if shift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shift.errors.count, "error") %> prohibited this shift from being saved:</h2>

      <ul>
      <% shift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="container">
  <!-- Muestra el panel de agregacion -->
      <div class="panel-group" >
          <div class="panel panel-default" >
            <!-- encabezado -->
              <div class="panel-heading ">
                <b class="panel-title">Turno</b>
                
              </div>
              <!-- formulario -->
              <div class="panel-body " >
                <div>&nbsp</div>
              <%= form.date_select :shift_date, id: :shift_shift_date, label:"Fecha de Turno" %>
              <div>&nbsp</div>
              <%= form.collection_select :user_id, User.all, :id, :name_with_initial, label: "Defensor" %>
              <!-- Botones -->
              <div>&nbsp</div>
              <div class="col-md-12">    
                      <%= button_tag(type: 'submit',class: "btn btn-primary pull-right") do %>
                        <%= fa_icon 'floppy-o' %> Guardar
                      <% end %> 
                      <%= link_to (fa_icon 'arrow-circle-left', text: 'Volver'),  shifts_path, class: 'btn btn-default pull-left' %>
                    </div>  
                

            <% end %>          
          </div> <!-- fin panel default -->
        </div> <!-- fin group -->
        </div>
        

   