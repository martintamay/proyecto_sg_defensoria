   
<div class="box">
            <div class="box-header with-border">
              <h3 class="box-title" >Notificación por Correo</h3>
              </div>
            
         <div class="box-body"> 
			<div>&nbsp;</div>
			<%= form_for :correo, :url => { :action => :create } do |f| %>
				<div class="col-md-12">
					<label for="email">Email:</label>
					<%= f.text_field :email, :class => "form-control", email: "true", required: "true" %>
				<div>&nbsp;</div>
					<label for="message">Tu mensaje:</label>
					<%= f.text_area :message, :class => "form-control", required: "true" %>
				</div>
				<div>&nbsp;</div>

				<div class="col-md-12">
    				<%= f.submit "Enviar", :class => 'btn btn-primary  fa-paper-plane pull-right ' %>
				</div>
			<% end %>
		</div>
</div>

